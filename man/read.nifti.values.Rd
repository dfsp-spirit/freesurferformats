% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nifti_common.R
\name{read.nifti.values}
\alias{read.nifti.values}
\title{Read raw voxel values of a NIFTI v1/v2 or ANALYZE file from a connection.}
\usage{
read.nifti.values(fh, datatype, bitpix, num_values, endian)
}
\arguments{
\item{fh}{connection to read from, positioned at the first value.}

\item{datatype}{integer, the \code{datatype} header field.}

\item{bitpix}{integer, the \code{bitpix} header field.}

\item{num_values}{integer, the number of values to read.}

\item{endian}{character string, the endianness of the file, 'little' or 'big'.}
}
\value{
numeric or integer vector of length \code{num_values}, the raw values as they are stored in the file.
}
\description{
Read \code{num_values} voxel values of the data type described by the \code{datatype} and \code{bitpix} fields of
a header, in the endianness of the file. This is the shared low level reading code of the NIFTI and ANALYZE
readers.
}
\note{
The signedness of the data type is taken from \code{\link{nifti.dtype.info}}: an unsigned 8 bit value of
200 is returned as 200, not as -56. R's \code{readBin} reads integers as signed by default, and it silently ignores
a \code{signed} argument for 4 byte integers, so the three unsigned types need special care.
}
\keyword{internal}
