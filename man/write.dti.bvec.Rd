% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_dwi_gradients.R
\name{write.dti.bvec}
\alias{write.dti.bvec}
\title{Write b-vectors to a FSL-style bvecs file.}
\usage{
write.dti.bvec(filepath, bvec, layout = "components")
}
\arguments{
\item{filepath}{character string, path to the output file. The conventional
extension is \code{.bvec} or \code{.bvecs}, but that is not enforced. Files ending in
\code{.gz} are gzip-compressed.}

\item{bvec}{numeric matrix with 3 columns (one row per volume) or 3 rows (one
column per volume), the gradient direction of each volume.}

\item{layout}{character string, one of 'components' or 'volumes'.
\code{components} (the default) writes the file with 3 lines, one per vector
component, which is the FSL layout, while \code{volumes} writes one volume per
line, which is what the Human Connectome Project distributes.}
}
\value{
NULL, invisibly. Called for the side effect of writing the file.
}
\description{
Writes the diffusion gradient directions of a diffusion MRI
dataset to a plain text file, which is the \emph{bvecs} part of the FSL
bvecs/bvals format. Note that these vectors are interpreted relative to the
\emph{image} axes, so the file is only meaningful together with the image it was
derived from.
}
\note{
A square 3x3 input is ambiguous. Since a matrix passed in memory
follows the R convention of one row per volume, the rows are written as the
volumes here; \code{\link[freesurferformats]{read.dti.bvec}} assumes the
components for a square file, so reading such a file back yields the
transposed matrix and warns about the ambiguity.
}
\seealso{
Other dti functions: 
\code{\link{read.dti.bval}()},
\code{\link{read.dti.bvec}()},
\code{\link{read.dti.grad}()},
\code{\link{read.dti.gradients}()},
\code{\link{write.dti.bval}()},
\code{\link{write.dti.grad}()}
}
\concept{dti functions}
