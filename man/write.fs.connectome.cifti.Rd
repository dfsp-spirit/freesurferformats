% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_cifti_connectome.R
\name{write.fs.connectome.cifti}
\alias{write.fs.connectome.cifti}
\title{Write a CIFTI-2 connectome file.}
\usage{
write.fs.connectome.cifti(
  filepath,
  data,
  template = NULL,
  axes = NULL,
  metadata = NULL
)
}
\arguments{
\item{filepath}{character string, the path of the file to write. The name should be one
of \code{.dconn.nii}, \code{.pconn.nii}, \code{.pdconn.nii} or \code{.dpconn.nii} (the file type is derived
from the axes, and the name has to agree with it).}

\item{data}{numeric matrix, the connectome: the first matrix dimension (the rows) is
CIFTI matrix dimension 0, which the axes describe. An \code{fs.connectome} instance (see
\code{\link{read.fs.connectome.cifti}}) is accepted as well, in which case its data and
its header are used.}

\item{template}{character string (the path of a CIFTI-2 file), an \code{fs.cifti} or an
\code{fs.cifti.data} object, or \code{NULL}. The axes of this file are used; a file with a single
brainordinate dimension has that mapping used for both dimensions of the connectome.}

\item{axes}{list of two axes (see \code{\link{cifti.header.from.axes}}) or a single axis
that is used for both dimensions, or \code{NULL}. Use this instead of \code{template} to write a
file with a mapping that is not in a file, e.g. a parcels axis built with
\code{\link{cifti.axis.parcels.from.annot}}.}

\item{metadata}{named character vector or named list, or \code{NULL}, the matrix metadata, see
\code{\link{write.cifti}}. The default keeps the metadata of the template file.}
}
\value{
the axes that were written, invisibly.
}
\description{
Write a connectome matrix to a CIFTI-2 file: a dense connectome
(\code{.dconn}), a parcellated connectome (\code{.pconn}), or one of the mixed types (\code{.pdconn},
\code{.dpconn}) whose two dimensions hold brainordinates and parcels. The file type follows
from the axes (and has to match the file name, see \code{\link{write.cifti}}), and the
mapping of the two dimensions has to be given: it cannot be derived from the matrix,
and guessing it (e.g. from the size of the matrix) would silently produce a file whose
rows and columns describe the wrong brain regions.

Use \code{template} for the normal case, i.e. to write a connectome of the brainordinates or
parcels of an existing file: this is how a \code{.dconn} of a subject gets the mapping of
that subject, and how a \code{.pconn} gets the parcels of the \code{.ptseries} it was computed
from. A template with a single brainordinate dimension (a \code{.dscalar}, \code{.dtseries} or
\code{.dlabel}, i.e. the files that actually exist for a subject) is accepted as well: its
mapping is then used for both dimensions of the connectome, which is what those files
describe. Use \code{axes} if the mapping has to be built, e.g. a parcels axis from
annotations (see \code{\link{cifti.axis.parcels.from.annot}}).

The complete matrix is written: a connectome file stores both halves of the matrix and
its diagonal, so the symmetry of the matrix is not exploited anywhere, and the matrix
of a \code{.dconn} can be huge (33 GB for the 91,282 grayordinates of an HCP subject, which
is more than any implementation can hold in memory). Writing such a file is not
supported yet; a connectome of a reduced set of brainordinates can be written.
}
\examples{
pconn_file <- system.file("extdata", "cifti", "tiny.pconn.nii", package = "freesurferformats")
conn <- read.fs.connectome.cifti(pconn_file)
out_file <- file.path(tempdir(), "written.pconn.nii")
# The values are squared, the mapping is the one of the original file:
write.fs.connectome.cifti(out_file, conn$data^2, template = pconn_file)
read.fs.connectome.cifti(out_file)$data[1:2, 1:2]
\dontshow{unlink(out_file)}

\dontrun{
# The grayordinates of a subject are the same in all its dense files, so a dtseries
# can define the mapping of the dconn:
write.fs.connectome.cifti("sub-01_dconn.nii", connectivity_matrix,
                          template = "sub-01_task-rest_dtseries.nii")
}

}
\seealso{
Other cifti functions:
\code{\link[=cifti.axis.brain.models]{cifti.axis.brain.models()}},
\code{\link[=cifti.axis.from.template]{cifti.axis.from.template()}},
\code{\link[=cifti.axis.labels]{cifti.axis.labels()}},
\code{\link[=cifti.axis.parcels]{cifti.axis.parcels()}},
\code{\link[=cifti.axis.parcels.from.annot]{cifti.axis.parcels.from.annot()}},
\code{\link[=cifti.axis.scalars]{cifti.axis.scalars()}},
\code{\link[=cifti.axis.series]{cifti.axis.series()}},
\code{\link[=cifti.brain.model.surface]{cifti.brain.model.surface()}},
\code{\link[=cifti.brain.model.volume]{cifti.brain.model.volume()}},
\code{\link[=cifti.dim.labels]{cifti.dim.labels()}},
\code{\link[=cifti.file.type.for.axes]{cifti.file.type.for.axes()}},
\code{\link[=cifti.grayordinates]{cifti.grayordinates()}},
\code{\link[=cifti.header.from.axes]{cifti.header.from.axes()}},
\code{\link[=cifti.label.table]{cifti.label.table()}},
\code{\link[=cifti.parcel]{cifti.parcel()}},
\code{\link[=cifti.parcels]{cifti.parcels()}},
\code{\link[=cifti.series.info]{cifti.series.info()}},
\code{\link[=cifti.structure.data]{cifti.structure.data()}},
\code{\link[=cifti.structures]{cifti.structures()}},
\code{\link[=cifti.volume]{cifti.volume()}},
\code{\link[=print.fs.cifti]{print.fs.cifti()}},
\code{\link[=print.fs.cifti.data]{print.fs.cifti.data()}},
\code{\link[=print.fs.connectome]{print.fs.connectome()}},
\code{\link[=read.cifti]{read.cifti()}},
\code{\link[=read.cifti.header]{read.cifti.header()}},
\code{\link[=read.fs.connectome.cifti]{read.fs.connectome.cifti()}},
\code{\link[=write.cifti]{write.cifti()}},
\code{\link[=write.fs.morph.cifti]{write.fs.morph.cifti()}},
\code{\link[=write.fs.parcellated.cifti]{write.fs.parcellated.cifti()}},
\code{\link[=write.fs.parcellation.cifti]{write.fs.parcellation.cifti()}},
\code{\link[=write.fs.series.cifti]{write.fs.series.cifti()}}
}
\concept{cifti functions}
