% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dti_tracks_utils.R
\name{scan.dti.tract.file}
\alias{scan.dti.tract.file}
\title{Scan a tract file without holding its data in memory.}
\usage{
scan.dti.tract.file(filepath, want = c("count", "bbox"), chunk_values = 4e+06)
}
\arguments{
\item{filepath}{character string, path to the \code{TCK} or \code{TSF} file
to read. Gzip-compressed files (i.e., \code{.tck.gz}) are supported and the
compression is detected from the file content, not the file name.}

\item{want}{character vector, any combination of 'count' and 'bbox'.}

\item{chunk_values}{integer, number of payload values to read per chunk.}
}
\value{
named list with the entries that were requested:
\code{count} (integer, the number of tracts) and \code{bbox} (numeric
vector of length 6, \code{c(xmin, xmax, ymin, ymax, zmin, zmax)}), or
\code{NULL} for \code{bbox} if no point was found.
}
\description{
Streams through the tract file and either counts the tracts, or
computes the bounding box of all their points, or both. Nothing but the
current chunk is ever held in memory, so this works on files of any size.
}
\keyword{internal}
