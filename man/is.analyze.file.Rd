% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_analyze.R
\name{is.analyze.file}
\alias{is.analyze.file}
\title{Check whether a file is an ANALYZE 7.5 file.}
\usage{
is.analyze.file(filepath)
}
\arguments{
\item{filepath}{character string, the path to a \code{.hdr} file (or to a file whose name ends with the base name of
one, see \code{\link{analyze.pair.files}}).}
}
\value{
logical, whether the 348 byte header is an ANALYZE 7.5 header. This is the case when the header contains
neither of the two NIFTI v1 magic strings, i.e. when the magic is empty. Files that use the \code{ni1} magic are
NIFTI v1 pair files, see \code{\link{read.nifti1.header}}, and files with the \code{n+1} magic are single file NIFTI
v1 files.
}
\description{
Check whether a file is an ANALYZE 7.5 file.
}
\note{
Note that this function returns \code{TRUE} for any two-file header that does not carry a NIFTI magic, which is
what "ANALYZE 7.5" means in practice: these files were written by ANALYZE itself, by SPM, by AFNI or by
FreeSurfer. Only the fields of the ANALYZE specification are defined for them, but SPM stores extra
information in fields that ANALYZE leaves unused, see \code{\link{read.fs.volume.analyze}}.

A file path that does not describe an existing pair file (for example a single file NIFTI image, or a
file that does not exist) returns \code{FALSE} rather than an error.
}
\examples{
hdrfile <- system.file("extdata", "analyze", "tiny_u8.hdr",
  package = "freesurferformats", mustWork = TRUE
)
is.analyze.file(hdrfile)

}
