% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dwi_gradients.R
\name{read.dti.bval}
\alias{read.dti.bval}
\title{Read b-values from a FSL-style bvals file.}
\usage{
read.dti.bval(filepath, layout = "auto")
}
\arguments{
\item{filepath}{character string, path to the b-values file, typically
ending in \code{.bval} or \code{.bvals}, but without an extension in the HCP layout.}

\item{layout}{character string, one of 'auto', 'components' or 'volumes'.
\code{components} means that the file stores the values in a single line (the
FSL layout), \code{volumes} means one value per line (the HCP layout). With
'auto' (the default), the layout is derived from the file content.}
}
\value{
numeric vector of length \emph{n}, the b-value of each of the \emph{n} volumes.
}
\description{
Reads the b-values of a diffusion MRI dataset, i.e. the
diffusion weighting of each volume in units of s/mm^2. This implements the
\emph{bvals} part of the FSL format: a plain text file with one value per
volume, usually in a single row. The HCP variant, which stores one value
per line, is read as well.
}
\seealso{
Other dti functions: 
\code{\link{read.dti.bvec}()},
\code{\link{read.dti.grad}()},
\code{\link{read.dti.gradients}()},
\code{\link{write.dti.bval}()},
\code{\link{write.dti.bvec}()},
\code{\link{write.dti.grad}()}
}
\concept{dti functions}
