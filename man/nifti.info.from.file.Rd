% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nifti_to_mgh.R
\name{nifti.info.from.file}
\alias{nifti.info.from.file}
\title{Read the header information and the data of a NIFTI file into a plain list.}
\usage{
nifti.info.from.file(filepath)
}
\arguments{
\item{filepath}{character string, the path to a NIFTI v1 or v2 file. The file extension may be omitted, see
\code{\link{nifti.resolve.filepath}}.}
}
\value{
named list with the NIFTI header fields \code{magic}, \code{datatype}, \code{bitpix}, \code{dim} (the 8 entry NIFTI
\code{dim} field, its first entry the number of used dimensions), \code{glmin} (the field used to store the true
dimension for the FreeSurfer hack files, \code{-1} for NIFTI v2 files, which do not have it), \code{scl_slope},
\code{scl_inter}, \code{xyzt_units}, \code{pixdim} (the 8 entry NIFTI \code{pixdim} field, its first entry \code{qfac}),
\code{sform_code}, \code{srow_x}, \code{srow_y}, \code{srow_z}, \code{qform_code}, \code{quatern_b}, \code{quatern_c}, \code{quatern_d},
\code{qoffset_x}, \code{qoffset_y}, \code{qoffset_z}, and the data array in \code{data}.
}
\description{
This is the file reading part of \code{\link{read.fs.volume.nii}}. The returned list uses plain
entry names instead of the \code{oro.nifti} slot names, so that no code outside of this file depends on the
name of an \code{oro.nifti} slot. The data scaling fields of the NIFTI header are applied, see the details in
\code{\link{read.fs.volume.nii}}.
}
\seealso{
\code{\link{nifti.info.from.oro.instance}}
}
\keyword{internal}
