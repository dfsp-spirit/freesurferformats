% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_nrrd.R
\name{read.nrrd.header}
\alias{read.nrrd.header}
\title{Read the header of a file in NRRD format.}
\usage{
read.nrrd.header(filepath)
}
\arguments{
\item{filepath}{character string, path to the NRRD file (\code{.nrrd} or \code{.nhdr}).}
}
\value{
named list, the parsed header. The fields of the file are available under their
normalized names (e.g. \code{space_directions} for the field 'space directions'), and fields that
this function does not interpret are collected in the \code{other_fields} entry. The derived
entries are \code{vox2ras_matrix} (the 4x4 matrix that maps 0-based voxel indices to RAS+
coordinates, or NULL when the file states no geometry), \code{vox2ras_source} (how the matrix was
derived: 'space directions' or 'spacings'), \code{data_files} (the resolved paths of the data files
for a detached header, or NULL), \code{data_offset} (the byte offset of the data in the header
file), \code{gzipped_file} (whether the whole file is gzip-compressed), \code{header_size} (the size of
the header in bytes) and \code{dwi} (the parsed diffusion metadata, or NULL, see
\code{\link{read.fs.volume.nrrd}}).
}
\description{
Reads and parses the ASCII header of a NRRD file without touching the volume data.
This is cheap, and it can be used to inspect a file, or to check its geometry, before deciding
whether to read the data.
}
\examples{
nrrd_file <- system.file("extdata", "nrrd", "vol_u8_raw.nrrd",
  package = "freesurferformats", mustWork = TRUE
)
hdr <- read.nrrd.header(nrrd_file)
hdr$sizes
hdr$vox2ras_matrix

}
