% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cifti_parcels.R
\name{cifti.axis.parcels.from.annot}
\alias{cifti.axis.parcels.from.annot}
\title{Create a CIFTI-2 parcels axis from brain surface annotations.}
\usage{
cifti.axis.parcels.from.annot(
  annots,
  structure = NULL,
  parcel_names = NULL,
  default_label_name = "unknown"
)
}
\arguments{
\item{annots}{an \code{fs.annot} instance (see \code{\link{read.fs.annot}}), or a named list
of them, with the brain structures as the names (e.g.
\code{list(lh = lh_annot, rh = rh_annot)}).}

\item{structure}{character string or \code{NULL}, the brain structure of the annotation,
needed if \code{annots} is a single \code{fs.annot} instance instead of a named list.}

\item{parcel_names}{character vector or \code{NULL}, the names of the parcels to write, in
that order. This selects and orders the parcels of the axis; without it all regions of
the parcellation are used, in the order in which the label table of the annotation
lists them (the atlas order, not the order in which the vertices happen to be stored
in the mesh). Renaming is not supported, since the name is what the hemispheres of a
region are matched by.}

\item{default_label_name}{character string, the name to use for vertices whose label is
not in the label table of the annotation.}
}
\value{
an axis of type 'CIFTI_INDEX_TYPE_PARCELS', see
\code{\link{cifti.axis.parcels}}, to be passed to \code{\link{write.cifti}} or to one
of the writers that accept it (e.g. \code{\link{write.fs.parcellated.cifti}}).
}
\description{
A parcellated CIFTI-2 file stores no label table, it describes each parcel
by its name and by the vertices (per brain structure) and volume voxels it contains.
This function builds such a parcels axis from brain surface annotations (see
\code{\link{read.fs.annot}}), which assign a region label to every vertex of a
hemisphere, so that data that was computed per region of an atlas (a FreeSurfer
parcellation, a Schaefer atlas, ...) can be written to a parcellated file.

The vertices of a parcel are collected by the \emph{name} of the region, not by its label
key: the hemispheres of an atlas usually use the same names for the same region, but
the names often carry a hemisphere marker (e.g. \code{L_superiorfrontal} in one hemisphere
and \code{R_superiorfrontal} in the other, or \verb{7Networks_LH_Vis_1} and
\verb{7Networks_RH_Vis_1}), and a parcel of a parcellated CIFTI-2 file is a region that
spans the structures it occurs in. The markers are removed before the names are
compared, see \code{\link{cifti.region.name.without.hemisphere}} for the exact rules.
Vertices whose label is not in the label table of the annotation (the medial wall,
which is usually the 'unknown' region with key 0) form a parcel like any other, named
\code{default_label_name}: a parcellated file has no 'no value' case, and dropping the
medial wall silently would change the data.
}
\examples{
lh_annot_file <- system.file("extdata", "lh.aparc.annot.gz", package = "freesurferformats")
lh_annot <- read.fs.annot(lh_annot_file)
axis <- cifti.axis.parcels.from.annot(lh_annot, structure = "lh")
length(axis$parcels)
axis$parcels[[1L]]$name

}
\seealso{
Other cifti functions:
\code{\link[=cifti.axis.brain.models]{cifti.axis.brain.models()}},
\code{\link[=cifti.axis.from.template]{cifti.axis.from.template()}},
\code{\link[=cifti.axis.labels]{cifti.axis.labels()}},
\code{\link[=cifti.axis.parcels]{cifti.axis.parcels()}},
\code{\link[=cifti.axis.scalars]{cifti.axis.scalars()}},
\code{\link[=cifti.axis.series]{cifti.axis.series()}},
\code{\link[=cifti.brain.model.surface]{cifti.brain.model.surface()}},
\code{\link[=cifti.brain.model.volume]{cifti.brain.model.volume()}},
\code{\link[=cifti.dim.labels]{cifti.dim.labels()}},
\code{\link[=cifti.file.type.for.axes]{cifti.file.type.for.axes()}},
\code{\link[=cifti.grayordinates]{cifti.grayordinates()}},
\code{\link[=cifti.header.from.axes]{cifti.header.from.axes()}},
\code{\link[=cifti.label.table]{cifti.label.table()}},
\code{\link[=cifti.parcel]{cifti.parcel()}},
\code{\link[=cifti.parcels]{cifti.parcels()}},
\code{\link[=cifti.series.info]{cifti.series.info()}},
\code{\link[=cifti.structure.data]{cifti.structure.data()}},
\code{\link[=cifti.structures]{cifti.structures()}},
\code{\link[=cifti.volume]{cifti.volume()}},
\code{\link[=print.fs.cifti]{print.fs.cifti()}},
\code{\link[=print.fs.cifti.data]{print.fs.cifti.data()}},
\code{\link[=print.fs.connectome]{print.fs.connectome()}},
\code{\link[=read.cifti]{read.cifti()}},
\code{\link[=read.cifti.header]{read.cifti.header()}},
\code{\link[=read.cifti.rows]{read.cifti.rows()}},
\code{\link[=read.fs.connectome.cifti]{read.fs.connectome.cifti()}},
\code{\link[=write.cifti]{write.cifti()}},
\code{\link[=write.fs.connectome.cifti]{write.fs.connectome.cifti()}},
\code{\link[=write.fs.morph.cifti]{write.fs.morph.cifti()}},
\code{\link[=write.fs.parcellated.cifti]{write.fs.parcellated.cifti()}},
\code{\link[=write.fs.parcellation.cifti]{write.fs.parcellation.cifti()}},
\code{\link[=write.fs.series.cifti]{write.fs.series.cifti()}}
}
\concept{cifti functions}
