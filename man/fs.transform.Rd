% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transforms_common.R
\name{fs.transform}
\alias{fs.transform}
\title{Create an fs.transform instance.}
\usage{
fs.transform(
  matrix,
  space_in = NA_character_,
  space_out = NA_character_,
  voxel_base = NA_integer_,
  src = NULL,
  dst = NULL,
  format = NA_character_,
  source = NULL,
  type = NULL,
  ...
)
}
\arguments{
\item{matrix}{4x4 numerical matrix, the transformation matrix. Required.}

\item{space_in}{character string, the kind of coordinates the matrix maps from, one of 'voxel' (voxel indices
of a volume), 'ras' (world coordinates, right-anterior-superior in millimeters) or 'lps' (world coordinates,
left-posterior-superior in millimeters, used by ITK and ITK-based tools like ANTs). Use \code{NA} if the file did
not state the space.}

\item{space_out}{character string, the kind of coordinates the matrix maps to, see \code{space_in}.}

\item{voxel_base}{integer, either 0 or 1. The index of the first voxel in the coordinates the matrix consumes
and produces, i.e. 1 for the convention used by FreeSurfer and tkregister, and 0 for the one used by
NIfTI, FSL and ITK. Must be \code{NA} unless \code{space_in} or \code{space_out} is 'voxel', since the value is meaningless
for world coordinates.}

\item{src}{\code{NULL} or named list, the volume or template that the matrix maps from. See \code{volume.descriptor}.}

\item{dst}{\code{NULL} or named list, the volume or template that the matrix maps to.}

\item{format}{character string, the file format the transform was read from, one of 'lta', 'dat', 'xfm' or
'fslmat'. Use \code{NA} if the transform was not read from a file.}

\item{source}{\code{NULL} or character string, the path of the file the transform was read from.}

\item{type}{\code{NULL} or character string, the transform type as stated by the file format (e.g. 'Linear' for an
xfm file, or the numeric LTA type as a string). This is format-specific metadata and is not interpreted by
the package.}

\item{...}{additional named fields to store in the transform, e.g. the parsed header of the file it was read
from. They are not validated and are preserved for formats that carry extra metadata.}
}
\value{
an \code{fs.transform} instance.
}
\description{
An \code{fs.transform} is a linear transformation matrix together with the coordinate spaces that it
maps between. It is the common representation that all transformation file format readers and writers in this
package return and accept, so that a matrix read from a file always states what its numbers mean: a bare 4x4
matrix does not tell whether it maps voxel indices or world coordinates, in which direction it goes, or
whether the voxel indices are zero- or one-based.

The \code{matrix} field always maps from the source to the target, i.e., applying it to a coordinate means
\verb{target_coord = matrix \\\%*\\\% c(source_coord, 1)}, which is the same convention that
\code{\link{doapply.transform.mtx}} uses. The inverse of the transformation is never stored, see
\code{\link{invert.fs.transform}}.
}
\keyword{internal}
