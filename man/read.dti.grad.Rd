% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dwi_gradients.R
\name{read.dti.grad}
\alias{read.dti.grad}
\title{Read a gradient table in MRtrix3 format.}
\usage{
read.dti.grad(filepath, layout = "auto")
}
\arguments{
\item{filepath}{character string, path to the file. There is no fixed file
name convention, \code{grad.b} and \code{encoding.b} are common. Gzip-compressed
files are supported.}

\item{layout}{character string, one of 'auto', 'components' or 'volumes'.
\code{volumes} means one volume per line (4 values per line, the MRtrix layout),
\code{components} the transposed variant (4 lines). With 'auto' (the default),
the layout is derived from the matrix dimensions.}
}
\value{
a numeric matrix with \emph{n} rows and 4 columns named 'x', 'y', 'z' and
'b', one row per volume. The directions are in scanner space.
}
\description{
Reads a diffusion gradient table in MRtrix format: a plain text
file with one line per DWI volume, each line holding the four
space-separated values \verb{[ x y z b ]}, with the direction in \emph{scanner}
space and the b-value in s/mm^2. This is the format expected by the
\code{-grad} option of the MRtrix3 commands (and produced by
\code{-export_grad_mrtrix}), and it is also the format in which MRtrix3 stores
the table in the \code{dw_scheme} entry of a \code{.mif} header. A first line
consisting of a single integer is accepted as a volume count header.
}
\seealso{
Other dti functions: 
\code{\link{read.dti.bval}()},
\code{\link{read.dti.bvec}()},
\code{\link{read.dti.gradients}()},
\code{\link{write.dti.bval}()},
\code{\link{write.dti.bvec}()},
\code{\link{write.dti.grad}()}
}
\concept{dti functions}
