% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dwi_gradients.R
\name{validate.dti.gradients}
\alias{validate.dti.gradients}
\title{Validate and normalize a diffusion MRI gradient table.}
\usage{
validate.dti.gradients(bvec, bval, n_volumes = NULL)
}
\arguments{
\item{bvec}{character string (path to a b-vectors file), or a numeric matrix
with one row per volume (or one column per volume) and 3 columns.}

\item{bval}{character string (path to a b-values file), or a numeric vector
with one value per volume. Can be \code{NULL} if \code{bvec} identifies both files.}

\item{n_volumes}{scalar numeric or integer, the number of volumes in the DWI
image that the gradients belong to, used to check that the table matches
the image. Typically \code{dim(volume$data)[4]}. Set to \code{NULL} (the default) to
skip this check.}
}
\value{
a named list with the entries \code{bvec} (numeric matrix with \emph{n} rows
and 3 columns, one row per volume) and \code{bval} (numeric vector of length
\emph{n}).
}
\description{
Checks that a set of b-vectors and b-values is consistent,
replaces missing values by \code{b=0} volumes, and reports suspicious entries.
This is the validation used by
\code{\link[freesurferformats]{read.dti.gradients}}, but it can also be
called directly on gradients from any other source.
}
\note{
The rules for missing values match those of MRtrix3: a missing b-value
in a volume with a valid direction, or a missing direction in a volume with
a non-zero b-value, is an error, because such a volume cannot be
interpreted. Everything else is treated as a \code{b=0} volume. The norm of the
gradient vectors is never changed, and a b-value is never rescaled, because
that would silently alter the data.
}
\keyword{internal}
