% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_cifti.R
\name{write.cifti}
\alias{write.cifti}
\title{Write a CIFTI-2 file.}
\usage{
write.cifti(filepath, data, axes = NULL, template = NULL, metadata = NULL)
}
\arguments{
\item{filepath}{character string, the path of the file to write.}

\item{data}{numeric or integer matrix or array, the data. The dimensions have to match
the sizes of the axes, and the order is the same as for \code{\link{read.cifti}}: the
first array dimension is CIFTI matrix dimension 0. A plain vector is interpreted as an
array of that length (which is what you want for a file with a single map, e.g. a
\code{.dscalar} with one map).}

\item{axes}{list of axes, one per matrix dimension, or \code{NULL} (in which case \code{template}
has to be given). See \code{\link{cifti.header.from.axes}} for the naming rules.}

\item{template}{character string (the path of a CIFTI-2 file), an \code{fs.cifti} or an
\code{fs.cifti.data} object or \code{NULL}. The axes of this file are used for the dimensions
that \code{axes} does not describe, and also for the metadata if \code{metadata} is \code{NULL}.}

\item{metadata}{named character vector or named list, or \code{NULL}, the matrix metadata to
write. The default writes the metadata of the template file, if there is one, so that
the provenance of the source data is kept.}
}
\value{
the axes that were written, invisibly. The file is written as a side effect.
}
\description{
Write a data matrix and its axes to a CIFTI-2 file, i.e. a NIFTI-2 file
with the CIFTI-2 XML in a header extension. The axes describe what the matrix
dimensions contain; they can be given explicitly (see the \verb{cifti.axis.*()} functions),
taken from a template file (recommended for real data, see
\code{\link{cifti.axis.from.template}}), or both.

The file type (and thus the NIFTI intent code that identifies it) follows from the
axes: a scalars dimension and a brain model dimension make a \code{.dscalar} file, a series
dimension and a brain model dimension a \code{.dtseries} file, and so on. If the file name
has one of the standard CIFTI extensions, the axes have to match it: a file named
\code{.pdconn.nii} whose dimensions are not (brain models, parcels) is an error, not a
warning, because the file name is the only hint that other software has about the
content.
}
\note{
The data are written as 32 bit floating point values, which is what the reference
implementations write for CIFTI-2 files (including for label files, whose keys are
small integers). Values that need more than about 7 significant digits are rounded.
}
\examples{
axis_brain <- cifti.axis.brain.models(list(
  cifti.brain.model.surface("lh", 10L),
  cifti.brain.model.surface("rh", 12L)))
data_matrix <- matrix(1:22, nrow = 1L)
out_file <- file.path(tempdir(), "tiny_written.dscalar.nii")
write.cifti(out_file, data_matrix, axes = list(cifti.axis.scalars("my data"), axis_brain))
\dontshow{unlink(out_file)}
\dontrun{
# Write data with the mapping of an existing file (recommended for real data):
template <- "Conte69.MyelinAndCorrThickness.32k_fs_LR.dscalar.nii"
data_matrix <- read.cifti(template)$data
write.cifti("copy.dscalar.nii", data_matrix, template = template)
}

}
\seealso{
Other cifti functions:
\code{\link[=cifti.axis.brain.models]{cifti.axis.brain.models()}},
\code{\link[=cifti.axis.from.template]{cifti.axis.from.template()}},
\code{\link[=cifti.axis.labels]{cifti.axis.labels()}},
\code{\link[=cifti.axis.parcels]{cifti.axis.parcels()}},
\code{\link[=cifti.axis.scalars]{cifti.axis.scalars()}},
\code{\link[=cifti.axis.series]{cifti.axis.series()}},
\code{\link[=cifti.brain.model.surface]{cifti.brain.model.surface()}},
\code{\link[=cifti.brain.model.volume]{cifti.brain.model.volume()}},
\code{\link[=cifti.dim.labels]{cifti.dim.labels()}},
\code{\link[=cifti.file.type.for.axes]{cifti.file.type.for.axes()}},
\code{\link[=cifti.grayordinates]{cifti.grayordinates()}},
\code{\link[=cifti.header.from.axes]{cifti.header.from.axes()}},
\code{\link[=cifti.label.table]{cifti.label.table()}},
\code{\link[=cifti.parcel]{cifti.parcel()}},
\code{\link[=cifti.parcels]{cifti.parcels()}},
\code{\link[=cifti.series.info]{cifti.series.info()}},
\code{\link[=cifti.structure.data]{cifti.structure.data()}},
\code{\link[=cifti.structures]{cifti.structures()}},
\code{\link[=cifti.volume]{cifti.volume()}},
\code{\link[=print.fs.cifti]{print.fs.cifti()}},
\code{\link[=print.fs.cifti.data]{print.fs.cifti.data()}},
\code{\link[=read.cifti]{read.cifti()}},
\code{\link[=read.cifti.header]{read.cifti.header()}},
\code{\link[=write.fs.morph.cifti]{write.fs.morph.cifti()}},
\code{\link[=write.fs.parcellation.cifti]{write.fs.parcellation.cifti()}},
\code{\link[=write.fs.series.cifti]{write.fs.series.cifti()}}
}
\concept{cifti functions}
