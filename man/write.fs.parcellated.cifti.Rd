% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_cifti_connectome.R
\name{write.fs.parcellated.cifti}
\alias{write.fs.parcellated.cifti}
\title{Write a CIFTI-2 parcellated map or series file.}
\usage{
write.fs.parcellated.cifti(
  filepath,
  data,
  template = NULL,
  axes = NULL,
  map_names = NULL,
  start = 0,
  step = 1,
  unit = "SECOND",
  metadata = NULL
)
}
\arguments{
\item{filepath}{character string, the path of the file to write, ending in
\code{.pscalar.nii} or \code{.ptseries.nii}.}

\item{data}{numeric vector or matrix, the data: a vector with one value per parcel (a
\code{.pscalar} with a single map), or a matrix with one row per map (for a \code{.pscalar}) or
series point (for a \code{.ptseries}) and one column per parcel. The first matrix dimension
is CIFTI matrix dimension 0, like for every other reader and writer of this package.}

\item{template}{character string (the path of a CIFTI-2 file), an \code{fs.cifti} or an
\code{fs.cifti.data} object, or \code{NULL}. The parcels of this file are used, so pass the
parcellated file the data belongs to (e.g. a \code{.ptseries} or \code{.pconn} of the same
subject).}

\item{axes}{list of two axes or a single parcels axis (see
\code{\link{cifti.axis.parcels}}), or \code{NULL}. Use this instead of \code{template} to write
data for a parcellation that is not in a CIFTI-2 file, e.g. one built from annotations
with \code{\link{cifti.axis.parcels.from.annot}}.}

\item{map_names}{character vector or \code{NULL}, the names of the maps of a \code{.pscalar}. The
number of names has to match the number of rows of the data. Without this, the map
names of the template are kept if it is a \code{.pscalar} with as many maps as the data.}

\item{start}{numeric, the value of the first series point of a \code{.ptseries}, see
\code{\link{cifti.axis.series}}.}

\item{step}{numeric, the difference between consecutive series points.}

\item{unit}{character string, the unit of the series, one of 'SECOND', 'HERTZ', 'METER'
or 'RADIAN'.}

\item{metadata}{named character vector or named list, or \code{NULL}, the matrix metadata, see
\code{\link{write.cifti}}. The default keeps the metadata of the template file.}
}
\value{
the axes that were written, invisibly.
}
\description{
Write data that has one value (or one time series) per parcel to a
parcellated CIFTI-2 file: a \code{.pscalar} (scalar maps, e.g. a mean value per region) or a
\code{.ptseries} (a time series per region). The parcellated dimension of these file types
is matrix dimension 1, and the order of the parcels is the order of the parcels axis,
which comes from a template file or from a parcels axis that you build (see
\code{\link{cifti.axis.parcels.from.annot}} for a parcellation of this package, e.g.
the annotations of a FreeSurfer atlas).

The file type is derived from the file name: a name ending in \code{.pscalar.nii} gets a
scalars dimension (the map names come from \code{map_names}), one ending in \code{.ptseries.nii}
a series dimension (described by \code{start}, \code{step} and \code{unit}). Use
\code{\link{write.cifti}} directly for the other parcellated types (\code{.pconn}, the
mixed connectome types) or for a file with a non-standard name.
}
\examples{
template <- system.file("extdata", "cifti", "tiny.ptseries.nii", package = "freesurferformats")
data <- matrix(seq_len(3 * 4), nrow = 4L) # 4 series points, 3 parcels
out_file <- file.path(tempdir(), "written.ptseries.nii")
write.fs.parcellated.cifti(out_file, data, template = template, step = 0.5)
dim(read.cifti(out_file)$data)
\dontshow{unlink(out_file)}

}
\seealso{
Other cifti functions:
\code{\link[=cifti.axis.brain.models]{cifti.axis.brain.models()}},
\code{\link[=cifti.axis.from.template]{cifti.axis.from.template()}},
\code{\link[=cifti.axis.labels]{cifti.axis.labels()}},
\code{\link[=cifti.axis.parcels]{cifti.axis.parcels()}},
\code{\link[=cifti.axis.parcels.from.annot]{cifti.axis.parcels.from.annot()}},
\code{\link[=cifti.axis.scalars]{cifti.axis.scalars()}},
\code{\link[=cifti.axis.series]{cifti.axis.series()}},
\code{\link[=cifti.brain.model.surface]{cifti.brain.model.surface()}},
\code{\link[=cifti.brain.model.volume]{cifti.brain.model.volume()}},
\code{\link[=cifti.dim.labels]{cifti.dim.labels()}},
\code{\link[=cifti.file.type.for.axes]{cifti.file.type.for.axes()}},
\code{\link[=cifti.grayordinates]{cifti.grayordinates()}},
\code{\link[=cifti.header.from.axes]{cifti.header.from.axes()}},
\code{\link[=cifti.label.table]{cifti.label.table()}},
\code{\link[=cifti.parcel]{cifti.parcel()}},
\code{\link[=cifti.parcels]{cifti.parcels()}},
\code{\link[=cifti.series.info]{cifti.series.info()}},
\code{\link[=cifti.structure.data]{cifti.structure.data()}},
\code{\link[=cifti.structures]{cifti.structures()}},
\code{\link[=cifti.volume]{cifti.volume()}},
\code{\link[=print.fs.cifti]{print.fs.cifti()}},
\code{\link[=print.fs.cifti.data]{print.fs.cifti.data()}},
\code{\link[=print.fs.connectome]{print.fs.connectome()}},
\code{\link[=read.cifti]{read.cifti()}},
\code{\link[=read.cifti.header]{read.cifti.header()}},
\code{\link[=read.fs.connectome.cifti]{read.fs.connectome.cifti()}},
\code{\link[=write.cifti]{write.cifti()}},
\code{\link[=write.fs.connectome.cifti]{write.fs.connectome.cifti()}},
\code{\link[=write.fs.morph.cifti]{write.fs.morph.cifti()}},
\code{\link[=write.fs.parcellation.cifti]{write.fs.parcellation.cifti()}},
\code{\link[=write.fs.series.cifti]{write.fs.series.cifti()}}
}
\concept{cifti functions}
