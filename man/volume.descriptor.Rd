% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transforms_common.R
\name{volume.descriptor}
\alias{volume.descriptor}
\title{Describe a volume for an fs.transform.}
\usage{
volume.descriptor(
  path = NULL,
  dim = NULL,
  voxelsize = NULL,
  xras = NULL,
  yras = NULL,
  zras = NULL,
  cras = NULL,
  vox2ras = NULL,
  frame = "scanner",
  valid = NULL
)
}
\arguments{
\item{path}{\code{NULL} or character string, the file name of the volume as recorded in the transform file.}

\item{dim}{\code{NULL} or integer vector of length 3, the volume dimensions (number of voxels along each axis).}

\item{voxelsize}{\code{NULL} or numerical vector of length 3, the size of a voxel in mm along each axis.}

\item{xras}{\code{NULL} or numerical vector of length 3, the RAS direction of the first voxel axis, scaled by the
voxel size. This is the 'xras' entry of the volume info section of an LTA file.}

\item{yras}{\code{NULL} or numerical vector of length 3, the RAS direction of the second voxel axis, scaled by the
voxel size.}

\item{zras}{\code{NULL} or numerical vector of length 3, the RAS direction of the third voxel axis, scaled by the
voxel size.}

\item{cras}{\code{NULL} or numerical vector of length 3, the RAS coordinates of the \strong{center} of the volume, i.e.
of voxel index \code{dim/2}. This is not the RAS coordinate of the first voxel, and using it as the translation
column of a voxel-to-RAS matrix is an error of half the field of view. The 'cras' entry of the volume info
section of an LTA file follows this convention, as verified against \code{mri_info --cras}.}

\item{vox2ras}{\code{NULL} or 4x4 numerical matrix, the transformation from voxel indices to RAS coordinates. If
not given but \code{dim}, \code{xras}, \code{yras}, \code{zras} and \code{cras} are, the matrix is computed from them in the
convention that \code{\link{mghheader.vox2ras}} implements for MGH headers: zero-based voxel indices and
the origin at \code{cras - Mdc_scaled * (dim/2)}. Verified against \code{mri_info --vox2ras} on a real volume.}

\item{frame}{character string, either 'scanner', 'tkreg' or 'fsl', see the description.}

\item{valid}{\code{NULL} or integer, the 'valid' flag of the volume info section of an LTA file, which states
whether the recorded geometry could be used by FreeSurfer.}
}
\value{
\code{NULL} if no information was given, a named list describing the volume otherwise.
}
\description{
Transformation files often record the geometry of the volumes they relate, either completely (an
LTA file stores the voxel dimensions, the voxel sizes and the voxel-to-RAS direction vectors of both volumes)
or not at all. This helper turns that information into the \code{src} and \code{dst} descriptors of an \code{fs.transform}.

The \code{frame} entry states which RAS coordinates the \code{vox2ras} matrix produces: 'scanner' means that the matrix
is used as given (this is what an LTA file records), and 'tkreg' means FreeSurfer tkregister coordinates,
which are RAS coordinates with an identity rotation and the origin at the center of the volume. A descriptor
may be partial, e.g. when only the target is known to be in tkregister space.
}
\keyword{internal}
