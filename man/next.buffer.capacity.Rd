% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dti_tcktsf.R
\name{next.buffer.capacity}
\alias{next.buffer.capacity}
\title{Compute the next capacity for a growing result buffer.}
\usage{
next.buffer.capacity(needed, capacity, bytes_per_elem = 8, label = NULL)
}
\arguments{
\item{needed}{numeric, the number of rows that the buffer must hold.}

\item{capacity}{numeric, the current capacity.}

\item{bytes_per_elem}{numeric, bytes per row (for integer vectors this is the
size of a single element).}

\item{label}{character string or NULL, description used in error messages.}
}
\value{
numeric, the new capacity.
}
\description{
Validates that the data actually needed fits into the configured
allocation limit, then returns the new buffer capacity to allocate. The
capacity grows geometrically to keep the number of reallocations
logarithmic, but it is capped at the limit: an overshoot of the growth must
not make a read fail that would have fit into memory, and it must not
exceed the limit either.
}
\keyword{internal}
