% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cifti_parcels.R
\name{cifti.region.name.without.hemisphere}
\alias{cifti.region.name.without.hemisphere}
\title{Remove hemisphere markers from region names of a parcellation.}
\usage{
cifti.region.name.without.hemisphere(region_names)
}
\arguments{
\item{region_names}{character vector, the region names.}
}
\value{
character vector, the names without hemisphere markers.
}
\description{
Region names of brain atlases often carry a marker that identifies the
hemisphere a region belongs to, e.g. \code{L_superiorfrontal} / \code{R_superiorfrontal},
\code{pericalcarine_LH} / \code{pericalcarine_RH}, or \verb{7Networks_LH_Vis_1} /
\verb{7Networks_RH_Vis_1}. The same region of the two hemispheres has to be recognized as
one parcel when building a parcel axis for a CIFTI-2 file, so these markers are
removed here.

A marker that is removed is one of \code{L}, \code{R}, \code{LH}, \code{RH}, \code{Left} or \code{Right} (matched
case-insensitively), at the start of the name or at its end, followed or preceded by
\verb{_} or \code{-}, or enclosed by two separators anywhere in the name (in which case the two
separators collapse into one). Names without such a marker are returned unchanged, and
so are names that consist of nothing but a marker.
}
\keyword{internal}
