% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_cifti_fs.R
\name{cifti.prepare.surface.data}
\alias{cifti.prepare.surface.data}
\title{Prepare per-vertex data for writing to a CIFTI-2 file.}
\usage{
cifti.prepare.surface.data(
  data,
  structure = NULL,
  template = NULL,
  what = "data"
)
}
\arguments{
\item{data}{numeric vector or matrix, or a named list of them, the per-vertex data.}

\item{structure}{character string or \code{NULL}, the structure name for the non-list case.}

\item{template}{character string, \code{fs.cifti}, \code{fs.cifti.data} or \code{NULL}, the template.}

\item{what}{character string, a description of the data, used in error messages.}
}
\value{
a named list with the entries 'matrix' (the data matrix to write), 'axes' (the
axes, with the brainordinate axis filled in and the other axis left \code{NULL}), 'template'
(the template object or \code{NULL}) and 'template_map_axis' (the axis of the template for
the other matrix dimension, or \code{NULL}).
}
\description{
Common work of the user-facing CIFTI-2 writers: turn data that are given
for the complete surface into the matrix of a dense CIFTI-2 file, i.e. one row per map
(or series point, or label map) and one column per brainordinate, in the order of the
grayordinates of the file.
}
\keyword{internal}
