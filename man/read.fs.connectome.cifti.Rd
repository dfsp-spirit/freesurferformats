% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_cifti_connectome.R
\name{read.fs.connectome.cifti}
\alias{read.fs.connectome.cifti}
\title{Read a CIFTI-2 connectome file.}
\usage{
read.fs.connectome.cifti(filepath, rows = NULL, columns = NULL)
}
\arguments{
\item{filepath}{character string, the path of a CIFTI-2 file, see \code{\link{read.cifti}}.
An \code{fs.cifti} object (see \code{\link{read.cifti.header}}) or an \code{fs.cifti.data} object
(see \code{\link{read.cifti}}) are accepted as well.}

\item{rows}{integer vector or \code{NULL}, the indices of matrix dimension 0 to read, see
\code{\link{read.cifti}}.}

\item{columns}{integer vector or \code{NULL}, the indices of matrix dimension 1 to read. This
is the way to read part of a file that is too large to read completely, see
\code{\link{read.cifti}}.}
}
\value{
a named list with class 'fs.connectome':
\itemize{
\item 'data': numeric matrix, the connectome, with the parcels or brainordinates as
the dimnames of its rows and columns (see \code{\link{cifti.dim.labels}}),
\item 'parcel_names': character vector, the names of the parcels, or \code{NULL} if the
file has no parcellated dimension,
\item 'parcels': data.frame with one row per parcel (the columns 'index', 'name',
'num_vertices' and 'num_voxels', see \code{\link{cifti.parcels}}), or \code{NULL},
\item 'parcels_dim': integer, the matrix dimension that holds the parcels (0 or 1),
or \code{NA} if the file has none,
\item 'grayordinates': data.frame with one row per brainordinate of the dense
dimension (see \code{\link{cifti.grayordinates}}), or \code{NULL},
\item 'grayordinates_dim': integer, the matrix dimension that holds the dense
brainordinates (0 or 1), or \code{NA} if the file has none,
\item 'header': the \code{fs.cifti} metadata object, see \code{\link{read.cifti.header}}.
}
Note that the parcels and brainordinates describe \emph{all} indices of the dimension they
belong to, not only the ones selected with \code{rows} or \code{columns}.
}
\description{
Read a CIFTI-2 file whose two matrix dimensions both describe
brainordinates or parcels, i.e. a dense connectome (\code{.dconn}), a parcellated
connectome (\code{.pconn}) or one of the mixed types (\code{.pdconn}, \code{.dpconn}). The result
contains the matrix and the parcels or brainordinates its rows and columns belong to;
this is what makes a connectome file usable, since the numbers alone do not say which
pair of regions a value describes.

Both values of a connectome are stored in the file (the matrix is not symmetric on
disk, and the diagonal is stored as well), so reading a \code{.pconn} gives a square matrix
with the number of parcels as its number of rows and columns. A real \code{.dconn} (an HCP
subject has 91,282 grayordinates, i.e. 8.3 billion values, 33 GB) can not be read into
memory at all: use the \code{rows} and \code{columns} parameters to read only the part you need,
which is a contiguous block of the file for each requested column.
}
\examples{
pconn_file <- system.file("extdata", "cifti", "tiny.pconn.nii", package = "freesurferformats")
conn <- read.fs.connectome.cifti(pconn_file)
dim(conn$data)
conn$parcel_names
conn$data[1:2, 1:2]

}
\seealso{
Other cifti functions:
\code{\link[=cifti.axis.brain.models]{cifti.axis.brain.models()}},
\code{\link[=cifti.axis.from.template]{cifti.axis.from.template()}},
\code{\link[=cifti.axis.labels]{cifti.axis.labels()}},
\code{\link[=cifti.axis.parcels]{cifti.axis.parcels()}},
\code{\link[=cifti.axis.parcels.from.annot]{cifti.axis.parcels.from.annot()}},
\code{\link[=cifti.axis.scalars]{cifti.axis.scalars()}},
\code{\link[=cifti.axis.series]{cifti.axis.series()}},
\code{\link[=cifti.brain.model.surface]{cifti.brain.model.surface()}},
\code{\link[=cifti.brain.model.volume]{cifti.brain.model.volume()}},
\code{\link[=cifti.dim.labels]{cifti.dim.labels()}},
\code{\link[=cifti.file.type.for.axes]{cifti.file.type.for.axes()}},
\code{\link[=cifti.grayordinates]{cifti.grayordinates()}},
\code{\link[=cifti.header.from.axes]{cifti.header.from.axes()}},
\code{\link[=cifti.label.table]{cifti.label.table()}},
\code{\link[=cifti.parcel]{cifti.parcel()}},
\code{\link[=cifti.parcels]{cifti.parcels()}},
\code{\link[=cifti.series.info]{cifti.series.info()}},
\code{\link[=cifti.structure.data]{cifti.structure.data()}},
\code{\link[=cifti.structures]{cifti.structures()}},
\code{\link[=cifti.volume]{cifti.volume()}},
\code{\link[=print.fs.cifti]{print.fs.cifti()}},
\code{\link[=print.fs.cifti.data]{print.fs.cifti.data()}},
\code{\link[=print.fs.connectome]{print.fs.connectome()}},
\code{\link[=read.cifti]{read.cifti()}},
\code{\link[=read.cifti.header]{read.cifti.header()}},
\code{\link[=read.cifti.rows]{read.cifti.rows()}},
\code{\link[=write.cifti]{write.cifti()}},
\code{\link[=write.fs.connectome.cifti]{write.fs.connectome.cifti()}},
\code{\link[=write.fs.morph.cifti]{write.fs.morph.cifti()}},
\code{\link[=write.fs.parcellated.cifti]{write.fs.parcellated.cifti()}},
\code{\link[=write.fs.parcellation.cifti]{write.fs.parcellation.cifti()}},
\code{\link[=write.fs.series.cifti]{write.fs.series.cifti()}}
}
\concept{cifti functions}
