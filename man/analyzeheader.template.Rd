% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_analyze.R
\name{analyzeheader.template}
\alias{analyzeheader.template}
\title{Create a template ANALYZE 7.5 header. You will have to adapt it for your use case.}
\usage{
analyzeheader.template()
}
\value{
named list, the ANALYZE 7.5 header. The fields \code{endian}, \code{magic}, \code{header_format} and \code{spm_origin} are not
part of the ANALYZE 7.5 header: \code{endian} and \code{magic} describe how the file is stored, \code{header_format} tells
which variant of the 348 byte header this is ('analyze' or 'nifti1_pair'), and \code{spm_origin} is the
interpretation of the \code{originator} field that the SPM software uses.
}
\description{
This function returns a valid ANALYZE 7.5 header with all fields present, filled with the default
values of the format. You will most likely have to adapt at least the \code{dim}, \code{datatype}, \code{bitpix} and \code{pix_dim}
fields to your data, which is what \code{\link{analyzeheader.for.data}} does for you.
}
\note{
The fields \code{spm_origin} and \code{magic} are derived when a file is read, they are documented in
\code{\link{read.analyze.header}}.
}
\examples{
analyzeheader <- analyzeheader.template()
analyzeheader$dim <- c(3L, 4L, 3L, 2L, 1L, 1L, 1L, 1L)
analyzeheader$pix_dim <- c(1., 1., 1., 1., 0., 0., 0., 0.)

}
\seealso{
\code{\link{analyzeheader.for.data}}, \code{\link{read.analyze.header}}, \code{\link{write.analyze}}
}
