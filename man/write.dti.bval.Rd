% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_dwi_gradients.R
\name{write.dti.bval}
\alias{write.dti.bval}
\title{Write b-values to a FSL-style bvals file.}
\usage{
write.dti.bval(filepath, bval, layout = "components")
}
\arguments{
\item{filepath}{character string, path to the output file. The conventional
extension is \code{.bval} or \code{.bvals}, but that is not enforced. Files ending in
\code{.gz} are gzip-compressed.}

\item{bval}{numeric vector, the b-value of each volume, in s/mm^2.}

\item{layout}{character string, one of 'components' or 'volumes'.
\code{components} (the default) writes all values into a single line, which is
what the FSL tools produce, while \code{volumes} writes one value per line,
which is what the Human Connectome Project distributes.}
}
\value{
NULL, invisibly. Called for the side effect of writing the file.
}
\description{
Writes the diffusion weighting of each volume of a diffusion
MRI dataset to a plain text file, which is the \emph{bvals} part of the FSL
bvecs/bvals format.
}
\seealso{
Other dti functions:
\code{\link[=read.dti.bval]{read.dti.bval()}},
\code{\link[=read.dti.bvec]{read.dti.bvec()}},
\code{\link[=read.dti.grad]{read.dti.grad()}},
\code{\link[=read.dti.gradients]{read.dti.gradients()}},
\code{\link[=write.dti.bvec]{write.dti.bvec()}},
\code{\link[=write.dti.grad]{write.dti.grad()}}
}
\concept{dti functions}
