% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_cifti_header.R
\name{cifti.file.looks.like.cifti2}
\alias{cifti.file.looks.like.cifti2}
\title{Check whether a file is a CIFTI-2 file (internal helper).}
\usage{
cifti.file.looks.like.cifti2(filepath)
}
\arguments{
\item{filepath}{character string, the path to a CIFTI-2 file (usually one of
\code{.dscalar.nii}, \code{.dtseries.nii}, \code{.dlabel.nii}, \code{.dconn.nii}, \code{.pscalar.nii},
\code{.ptseries.nii}, \code{.pconn.nii}, \code{.dpconn.nii} or \code{.pdconn.nii}). Note that
this is not a NIFTI file, despite the \code{.nii} part. Gzipped CIFTI files are
not supported, because the CIFTI-2 format forbids compression.}
}
\value{
logical, whether the file is a CIFTI-2 file. A file that does not exist or
cannot be parsed is reported as \code{FALSE}.
}
\description{
Cheap and silent check whether a file is a CIFTI-2 file, i.e. a NIFTI v2
file with a header extension of code 32, the extension that holds the CIFTI XML
metadata. The format dispatchers (\code{read.fs.morph()}, \code{read.fs.volume()}) use it to
give a helpful error: the payload of a CIFTI-2 file is a matrix whose dimensions the
XML describes, not a volume or a per-vertex vector, so reading it with the NIFTI
reader silently returns values in an order that means nothing.
}
\keyword{internal}
