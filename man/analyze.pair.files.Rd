% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_analyze.R
\name{analyze.pair.files}
\alias{analyze.pair.files}
\title{Determine the header and data file of an ANALYZE 7.5 or NIFTI v1 pair file.}
\usage{
analyze.pair.files(filepath, require_header = FALSE)
}
\arguments{
\item{filepath}{character string, the path to the header file, to the data file, or to the base name (with or
without a \code{.hdr}/\code{.img}/\code{.gz} suffix).}

\item{require_header}{logical, whether to stop with an error if the header file does not exist. If \code{FALSE}, the
computed file names are returned even if the files are missing.}
}
\value{
named list with the entries \code{header} and \code{image}, the full paths to the header and the data file, the
logical entries \code{header_exists} and \code{image_exists}, and the entries \code{mat} (the path of the MATLAB sidecar file
that SPM and FreeSurfer write next to the image file, see \code{\link{read.fs.volume.analyze}}) and
\code{mat_exists}, which report whether such a file is present.
}
\description{
The two formats ANALYZE 7.5 and NIFTI v1 (in its two-file variant) store the image in a header file
(\verb{<base>.hdr}) and a data file (\verb{<base>.img}). This function computes the two file names from any of them, so
that the user can pass any of the possible spellings.
}
\note{
Compression is handled the way the other implementations of the format handle it: the suffix \code{.gz} (and
\code{.hdr}/\code{.img}) is stripped from the file name to get the base name, and the two file names are constructed from
it. A name like \code{vol.hdr.gz} therefore describes the pair \code{vol.hdr.gz} and \code{vol.img.gz}, and \code{vol.nii.gz} would
be the pair \code{vol.nii.hdr.gz} and \code{vol.nii.img.gz}. Note that many tools only read uncompressed pairs, so
compressed pairs should only be written if the software that reads them supports this.
}
\keyword{internal}
