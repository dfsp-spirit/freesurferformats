% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nifti_common.R
\name{nifti.header.to.vox2ras}
\alias{nifti.header.to.vox2ras}
\title{Compute the voxel-to-RAS matrix from the geometry fields of a NIFTI v1 header.}
\usage{
nifti.header.to.vox2ras(niiheader)
}
\arguments{
\item{niiheader}{named list, a NIFTI v1 header as returned by \code{\link{read.nifti1.header}}.}
}
\value{
a 4x4 numeric matrix (the voxel-to-RAS transformation), or \code{NULL} if the header contains neither an
\code{sform} nor a \code{qform}. The matrix implements the NIFTI convention that the rotation of the \code{qform} applies to
the \emph{left} of the scaled voxel axes and that the third axis is flipped if the \code{qfac} field (\code{pix_dim[1]}) is
negative.
}
\description{
The NIFTI v1 header can store up to two descriptions of the mapping from voxel indices to world
coordinates: the \code{sform} (a full affine matrix in the \code{srow_x}, \code{srow_y} and \code{srow_z} fields) and the \code{qform} (a
rotation, given as a quaternion, plus a translation, given in the \code{quoffset} fields). Each of them is only valid
if the corresponding code field is not zero, and the \code{sform} takes precedence over the \code{qform} if both are
present. This is the same rule that the NIFTI standard defines and that other implementations (\code{nibabel},
\code{oro.nifti}) follow.
}
\note{
This is a header based re-implementation of the geometry computation of \code{\link{read.fs.volume.nii}},
which reads an \code{oro.nifti} instance. The two are compared against each other in the unit tests, and against
\code{nibabel} in \code{dev_tools/check_analyze_conversion.R}.
}
\keyword{internal}
