% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_analyze.R
\name{analyze.dtype.info}
\alias{analyze.dtype.info}
\title{Compute the R data type and the number of bytes per value for an ANALYZE data type.}
\usage{
analyze.dtype.info(analyzeheader, filepath = NULL)
}
\arguments{
\item{analyzeheader}{named list, an ANALYZE 7.5 header as returned by \code{\link{read.analyze.header}}.}

\item{filepath}{character string, the path of the file the header was read from. Only used for warning and error
messages.}
}
\value{
named list with the entries \code{datatype} and \code{bitpix} (the data type fields to use for reading the data,
which may differ from the values in the file if the file contradicts itself), \code{num_bytes} (the number of bytes
per value) and \code{r_dtype} (the R type to read the values as).
}
\description{
Compute the R data type and the number of bytes per value for an ANALYZE data type.
}
\note{
ANALYZE files in the wild sometimes have a wrong \code{bitpix} field, since the format was used by many tools
that did not care about it. The data type is the reliable field, so if the two fields contradict each other, the
\code{bitpix} value that belongs to the data type is used, with a warning.
}
\keyword{internal}
