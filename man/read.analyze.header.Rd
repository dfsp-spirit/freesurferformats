% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_analyze.R
\name{read.analyze.header}
\alias{read.analyze.header}
\title{Read ANALYZE 7.5 header from file.}
\usage{
read.analyze.header(filepath)
}
\arguments{
\item{filepath}{character string, the path to the \code{.hdr} file. The base name without the extension is accepted as
well, see \code{\link{analyze.pair.files}}.}
}
\value{
named list with the ANALYZE 7.5 header fields, in the field naming of the format specification. See
\code{\link{analyzeheader.template}} for a description of all of them. In addition to the fields of the
specification, the following entries are present:

\describe{
\item{\code{endian}}{character string, the endianness of the file, \code{'little'} or \code{'big'}. It is detected from the
\code{sizeof_hdr} field.}
\item{\code{magic}}{character string, the 4 bytes at offset 344 (which are the \code{smin} field of the ANALYZE
specification), interpreted as a string. It is empty for ANALYZE files, see \code{\link{is.analyze.file}}.}
\item{\code{header_format}}{character string, always \code{'analyze'} here. The other variant of the two-file format is
reported as \code{'nifti1_pair'}, see \code{\link{read.fs.volume.analyze}}.}
\item{\code{spm_origin}}{integer vector of length 3, the interpretation that SPM gives to the \code{originator} field:
the voxel coordinates of the image origin, as stored in the first 6 bytes of the field. It is \code{NULL} if all
of them are zero, which means that the field does not store an origin.}
\item{\code{originator_bytes}}{raw vector of length 10, the unmodified content of the \code{originator} field. The
\code{originator} entry is the same content with the zero bytes removed and interpreted as a string, which loses
information for the files in which SPM stores the image origin there.}
\item{\code{filepath_header}, \code{filepath_image}}{character strings, the paths of the two files of the pair.}
}
}
\description{
Read the header of an ANALYZE 7.5 file, i.e. of a file in the two-file format that stores the voxel
data in a separate \code{.img} file.
}
\note{
ANALYZE 7.5 does not define the meaning of the world coordinate system of an image: the header stores the
voxel sizes but neither the direction of the voxel axes nor the position of the image in space. This function
therefore reports the fields as they are stored and does not derive a transformation matrix. See
\code{\link{read.fs.volume.analyze}} for the ways to get a matrix anyway, and for what is known and unknown
about them.
}
\examples{
hdrfile <- system.file("extdata", "analyze", "tiny_u8.hdr",
  package = "freesurferformats", mustWork = TRUE
)
analyzeheader <- read.analyze.header(hdrfile)
analyzeheader$datatype
analyzeheader$pix_dim

}
\seealso{
\code{\link{read.analyze.data}}, \code{\link{analyzeheader.template}}
}
