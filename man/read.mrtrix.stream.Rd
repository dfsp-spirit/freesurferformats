% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dti_tcktsf.R
\name{read.mrtrix.stream}
\alias{read.mrtrix.stream}
\title{Read the data payload of an MRtrix TCK/TSF file.}
\usage{
read.mrtrix.stream(
  con,
  offset,
  dsize,
  endian,
  values_per_point,
  max_groups = Inf,
  chunk_values = 4e+06,
  filepath = "",
  gzipped = FALSE,
  expected_groups = NA_real_
)
}
\arguments{
\item{con}{a connection opened in binary read mode.}

\item{offset}{numeric, byte offset of the payload.}

\item{dsize}{integer, bytes per value (4 for Float32, 8 for Float64).}

\item{endian}{character string, 'little' or 'big'.}

\item{values_per_point}{integer, 3 for TCK (xyz triples) and 1 for TSF.}

\item{max_groups}{numeric, stop after this many groups. Use \code{Inf} to
read everything.}

\item{chunk_values}{integer, number of values to read per chunk.}

\item{filepath}{character string, the path of the file, used in messages only.}

\item{gzipped}{logical, whether the connection reads a gzip-compressed file.
R cannot seek on gzip connections, so the header bytes are then skipped by
reading and discarding them instead.}

\item{expected_groups}{numeric, the number of groups (streamlines) stated in
the header, or NA if the header does not contain a usable count. Only used
to warn about a possibly truncated file, never to limit reading.}
}
\value{
named list with entries \code{data} (matrix with
\code{values_per_point} columns holding the concatenated groups),
\code{lengths} (integer vector, number of points per group) and
\code{terminator_seen} (logical).
}
\description{
Reads the payload sequentially in chunks and splits it into
groups of \code{values_per_point} finite values, which are the points of the
individual streamlines. Non-finite groups act as separators, a group of
Inf values terminates the data. Groups that contain no points at all (two
consecutive separators, which MRtrix writes for empty streamlines) are
dropped, matching the 'nibabel' reader.
}
\keyword{internal}
