% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_dwi_gradients.R
\name{write.dti.grad}
\alias{write.dti.grad}
\title{Write a gradient table in MRtrix3 format.}
\usage{
write.dti.grad(filepath, bvec, bval = NULL, layout = "volumes")
}
\arguments{
\item{filepath}{character string, path to the output file. Files ending in
\code{.gz} are gzip-compressed.}

\item{bvec}{numeric matrix with 3 columns (one row per volume) or 3 rows (one
column per volume), the gradient directions. Alternatively a full gradient
table, i.e. a numeric matrix with 4 columns named or ordered 'x', 'y', 'z'
and 'b', as returned by
\code{\link[freesurferformats]{read.dti.grad}}. If a full table is given,
the \code{bval} parameter must be left at \code{NULL}.}

\item{bval}{numeric vector, the b-value of each volume, in s/mm^2. Ignored if
\code{bvec} is a full gradient table.}

\item{layout}{character string, one of 'volumes' or 'components'. \code{volumes}
(the default) writes one volume per line, which is the MRtrix layout, while
\code{components} writes the transposed variant with 4 lines.}
}
\value{
NULL, invisibly. Called for the side effect of writing the file.
}
\description{
Writes a diffusion gradient table to a plain text file with one
line per DWI volume, each line holding the four space-separated values
\verb{[ x y z b ]}. This is the format that the \code{-grad} option of the MRtrix3
commands expects, and the format of the \code{dw_scheme} entry in the header of
a \code{.mif} image.
}
\seealso{
Other dti functions: 
\code{\link{read.dti.bval}()},
\code{\link{read.dti.bvec}()},
\code{\link{read.dti.grad}()},
\code{\link{read.dti.gradients}()},
\code{\link{write.dti.bval}()},
\code{\link{write.dti.bvec}()}
}
\concept{dti functions}
