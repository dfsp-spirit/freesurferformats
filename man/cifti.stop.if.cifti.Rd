% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_cifti_header.R
\name{cifti.stop.if.cifti}
\alias{cifti.stop.if.cifti}
\title{Refuse to read a CIFTI file as a volume or morphometry file.}
\usage{
cifti.stop.if.cifti(filepath)
}
\arguments{
\item{filepath}{character string, the path to a CIFTI-2 file (usually one of
\code{.dscalar.nii}, \code{.dtseries.nii}, \code{.dlabel.nii}, \code{.dconn.nii}, \code{.pscalar.nii},
\code{.ptseries.nii}, \code{.pconn.nii}, \code{.dpconn.nii} or \code{.pdconn.nii}). Note that
this is not a NIFTI file, despite the \code{.nii} part. Gzipped CIFTI files are
not supported, because the CIFTI-2 format forbids compression.}
}
\value{
\code{NULL}, invisibly. Stops if the file is a CIFTI file.
}
\description{
The generic readers of this package dispatch on the file name, and a CIFTI
file is a NIFTI file as far as the name is concerned: \code{read.fs.morph()} used to
interpret a \code{.dscalar.nii} as a NIFTI morphometry file and silently returned the raw
matrix as a per-vertex vector (for the official Conte69 \code{.dtseries}, a 121,902 element
vector of a 60,951 x 2 matrix, i.e. plausible looking numbers in an order that means
nothing), and \code{read.fs.volume()} failed with the unrelated 'This is not a one-file
NIFTI format' message of \code{oro.nifti}. This function detects the CIFTI files and stops
with an error that names the reader to use instead, see \code{\link{read.cifti}}.

The check only costs a header read, and only for files whose name ends with \code{.nii} or
\code{.nii.gz}, since a CIFTI file always has such a name.
}
\keyword{internal}
