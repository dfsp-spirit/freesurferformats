% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_dti_tracks.R
\name{build.mrtrix.header.stable}
\alias{build.mrtrix.header.stable}
\title{Determine the length of an MRtrix streamlines header.}
\usage{
build.mrtrix.header.stable(header_id, entries, datatype, count)
}
\arguments{
\item{header_id}{character string, the file type identifier, one of 'mrtrix
tracks' (TCK) or 'mrtrix track scalars' (TSF).}

\item{entries}{named list of additional header entries.}

\item{datatype}{character string, the datatype.}

\item{count}{integer, the number of streamlines.}
}
\value{
named list with entries \code{text} (the header) and \code{offset}.
}
\description{
The length of the header determines the data offset that is
stored \emph{inside} the header, so the two have to be reconciled: the offset is
written with a number of digits that depends on its own value, which can
change the length of the header. Starting from an offset guess and
recomputing until it is stable always terminates, since the length only
ever grows with the number of digits and that number is bounded.
}
\keyword{internal}
