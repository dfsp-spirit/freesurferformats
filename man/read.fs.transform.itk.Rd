% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_fs_transform.R
\name{read.fs.transform.itk}
\alias{read.fs.transform.itk}
\title{Read a transformation matrix from an ITK text transform file.}
\usage{
read.fs.transform.itk(filepath)
}
\arguments{
\item{filepath}{character string, the full path to the transform file.}
}
\value{
an \code{fs.transform} instance. An ITK transform operates on the world coordinates of the images, which in
ITK are left-posterior-superior, so \code{space_in} and \code{space_out} are 'lps' and \code{voxel_base} is \code{NA}. This is
not the RAS space that the other formats of this package use, and it is not converted silently: use
\code{\link{transform.to.ras}} to get a transformation in RAS coordinates. The volumes are not recorded in
the file, so \code{src} and \code{dst} are \code{NULL}. The ITK class name (e.g. 'AffineTransform_float_3_3') is stored in
the \code{type} field, and the values of the \code{FixedParameters} entry in the \code{fixed_parameters} field.
}
\description{
Read the plain text file format that ITK and the tools built on it (3D Slicer, ANTs via
\code{ConvertTransformFile}, SimpleITK and the workflows of fMRIPrep/QSIPrep that are based on them) use to store
linear transformations, usually with the extension \code{.tfm} or \code{.txt}.
}
\note{
The format can store many kinds of transforms besides affine ones; this function reads the affine
transformations only, i.e. the classes 'AffineTransform_float_3_3', 'AffineTransform_double_3_3',
'MatrixOffsetTransformBase_float_3_3' and 'MatrixOffsetTransformBase_double_3_3'. These are the classes
that occur in the output of the pipelines mentioned above, and the only ones for which the interpretation
of the parameters could be verified against other implementations. Files that contain several transformations
(an ITK 'CompositeTransform') are not supported either, and are reported as such: composing them requires
the ordering rules of ITK, which would be a guess without a reference to check against.

The \code{FixedParameters} entry is the center of rotation, so the matrix that is returned is
\code{y = A(x - c) + t + c}, i.e. it has the center folded in. That is the same thing that the ITK writer of
this package stores, and the transformation is not changed by it.

FreeSurfer reads ITK files as well, but two limitations of its version 7.4.1 are worth knowing when the file
has to be passed to it: it rejects the 'float' variant of the classes ('readITK: Transform type unknown!'),
and its \code{lta_convert --initk} ignores the \code{FixedParameters}, so it interprets a file with a non-zero center
of rotation differently from ITK itself (which computes \code{offset = translation + center - matrix * center},
see \code{ComputeOffset()} in ITK's \code{itkMatrixOffsetTransformBase.hxx}) and from this package. Both were verified
by converting files that encode the same transformation, and both are avoided by the files that
\code{\link{write.fs.transform.itk}} writes.
}
\examples{
xfm_file <- system.file("extdata", "talairach.xfm", package = "freesurferformats", mustWork = TRUE)
tf <- transform.to.ras(transform.to.lps(read.fs.transform(xfm_file)))
summary(tf)$space_in

}
\seealso{
Other header coordinate space:
\code{\link[=invert.fs.transform]{invert.fs.transform()}},
\code{\link[=is.fs.transform]{is.fs.transform()}},
\code{\link[=mghheader.is.ras.valid]{mghheader.is.ras.valid()}},
\code{\link[=mghheader.ras2vox]{mghheader.ras2vox()}},
\code{\link[=mghheader.ras2vox.tkreg]{mghheader.ras2vox.tkreg()}},
\code{\link[=mghheader.scanner2tkreg]{mghheader.scanner2tkreg()}},
\code{\link[=mghheader.tkreg2scanner]{mghheader.tkreg2scanner()}},
\code{\link[=mghheader.vox2ras]{mghheader.vox2ras()}},
\code{\link[=mghheader.vox2ras.tkreg]{mghheader.vox2ras.tkreg()}},
\code{\link[=print.fs.transform]{print.fs.transform()}},
\code{\link[=read.fs.transform]{read.fs.transform()}},
\code{\link[=read.fs.transform.dat]{read.fs.transform.dat()}},
\code{\link[=read.fs.transform.fslmat]{read.fs.transform.fslmat()}},
\code{\link[=read.fs.transform.lta]{read.fs.transform.lta()}},
\code{\link[=read.fs.transform.xfm]{read.fs.transform.xfm()}},
\code{\link[=sm0to1]{sm0to1()}},
\code{\link[=sm1to0]{sm1to0()}},
\code{\link[=summary.fs.transform]{summary.fs.transform()}},
\code{\link[=transform.to.lps]{transform.to.lps()}},
\code{\link[=transform.to.ras]{transform.to.ras()}},
\code{\link[=transform.to.voxel]{transform.to.voxel()}},
\code{\link[=transform.to.world]{transform.to.world()}},
\code{\link[=write.fs.transform]{write.fs.transform()}},
\code{\link[=write.fs.transform.dat]{write.fs.transform.dat()}},
\code{\link[=write.fs.transform.fslmat]{write.fs.transform.fslmat()}},
\code{\link[=write.fs.transform.itk]{write.fs.transform.itk()}},
\code{\link[=write.fs.transform.lta]{write.fs.transform.lta()}},
\code{\link[=write.fs.transform.xfm]{write.fs.transform.xfm()}}
}
\concept{header coordinate space}
