% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nifti2_extensions.R
\name{nifti2.extension}
\alias{nifti2.extension}
\title{Create a NIFTI v2 header extension.}
\usage{
nifti2.extension(ecode, content = NULL)
}
\arguments{
\item{ecode}{integer, the extension code. This identifies the type of the payload, but the code is not interpreted in any way. The CIFTI2 XML metadata uses code \code{32L}.}

\item{content}{raw vector, character string, or \code{NULL}. The payload of the extension, i.e., the data to store. A character string is converted to UTF-8 bytes (several strings are joined with newline characters). Use \code{NULL} or omit for an empty payload.}
}
\value{
named list with the entries 'ecode' and 'content', representing one NIFTI v2 header extension.
}
\description{
NIFTI v2 files can store arbitrary data in header extensions, in the area between the header and the voxel data. This function creates such an extension. Pass the result to \code{\link{write.nifti2}}, which writes it to the file, and later read it back with \code{\link{read.nifti2.header}} plus \code{\link{nifti2.get.extension}}.
}
\seealso{
\code{\link{nifti2.get.extension}}, \code{\link{nifti2.extension.text}}

Other nifti2 extensions:
\code{\link[=nifti2.extension.content]{nifti2.extension.content()}},
\code{\link[=nifti2.extension.size]{nifti2.extension.size()}},
\code{\link[=nifti2.extension.text]{nifti2.extension.text()}},
\code{\link[=nifti2.get.extension]{nifti2.get.extension()}}
}
\concept{nifti2 extensions}
