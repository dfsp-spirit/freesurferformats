# Parse a quoted NRRD string list.

Parse a quoted NRRD string list.

## Usage

``` r
nrrd.parse.quoted.list(value)
```

## Arguments

- value:

  character string, the field value, e.g. 'left right' or '"some
  label"'.

## Value

character vector.
