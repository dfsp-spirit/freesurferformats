# Print an fs.tracts instance.

Print an fs.tracts instance.

## Usage

``` r
# S3 method for class 'fs.tracts'
print(x, ...)
```

## Arguments

- x:

  an `fs.tracts` instance.

- ...:

  ignored.

## Value

the instance, invisibly.
