# Print a tract file iterator.

Print a tract file iterator.

## Usage

``` r
# S3 method for class 'dti.track.iterator'
print(x, ...)
```

## Arguments

- x:

  a `dti.track.iterator` instance.

- ...:

  ignored.

## Value

the iterator, invisibly.
