# Check whether object is an fs.volume

Check whether object is an fs.volume

## Usage

``` r
is.fs.volume(x)
```

## Arguments

- x:

  any `R` object

## Value

TRUE if its argument is a brain volume (that is, has "fs.volume" amongst
its classes) and FALSE otherwise.
