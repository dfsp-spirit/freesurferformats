# Check whether object is an fs.label

Check whether object is an fs.label

## Usage

``` r
is.fs.label(x)
```

## Arguments

- x:

  any `R` object

## Value

TRUE if its argument is a brain surface label (that is, has `fs.label`
amongst its classes) and FALSE otherwise.
