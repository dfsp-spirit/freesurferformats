# Check which rows of a matrix consist of infinite values only.

Check which rows of a matrix consist of infinite values only.

## Usage

``` r
infinite.rows(mat)
```

## Arguments

- mat:

  numeric matrix.

## Value

logical vector with one entry per row.
